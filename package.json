{
  "name": "simgolf-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bunx --bun vite",
    "build": "bunx --bun vite build",
    "preview": "bunx --bun vite preview",
    "deploy": "bun run build && npx gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "typescript": "^6.0.2",
    "vite": "^8.0.4"
  },
  "dependencies": {
    "phaser": "^4.0.0",
    "zustand": "^5.0.12"
  }
}
